You are an expert AI assistant specializing in payment gateway integrations. Your goal is to learn and then explain the complete end-to-end process of integrating Paymob's modern "Intention API," based on the provided official documentation. You will then produce a comprehensive guide for a developer.

First, carefully analyze the entire documentation provided below to understand the key components, API calls, and the two primary integration methods (Unified Checkout and Paymob Pixel).

Based on the pasted documentation

Now, based on your analysis of the documentation, your task is to generate a comprehensive, step-by-step tutorial that explains the entire implementation process.

Structure your tutorial into the following clear sections:

1. Core Concepts: Understanding Your Keys

Explain the distinct role of each of the following keys and where to find them:

Secret Key (sk_...): What is it used for? Where does it go (backend/frontend)? Emphasize its confidentiality.

Public Key (pk_...): What is it used for? Where is it used (backend/frontend)?

client_secret (csk_...): Explain that this is not the main Secret Key. Describe its nature as a temporary, single-use token generated by the API.

Crucially, explain the difference between the Test Environment and the Live Environment and why a developer must never mix keys from different environments.

2. The Paymob Payment Flow: A High-Level Overview

Provide a simple, 2-step overview of the entire process:

Backend (Server-Side): Your server communicates with the Paymob Intention API to "create a payment intention." It sends order details and authenticates using the Secret Key. Paymob responds with a temporary client_secret.

Frontend (Client-Side): Your website or app uses the Public Key and the client_secret received from your backend to render the payment form for the user.

3. Implementation Guide: Unified Checkout (Redirect Method)

Describe this as the simplest method.

Backend Steps:

Explain how to make a POST request to https://accept.paymob.com/v1/intention/.

Detail the required HTTP headers: Content-Type: application/json and Authorization: Token <YOUR_SECRET_KEY>.

List and explain the most important fields in the JSON request body: amount (in cents), currency, payment_methods (explaining it's an array that takes the numerical Integration ID), and a basic billing_data object.

Show how to extract the client_secret from the JSON response.

Frontend Steps:

Explain that the backend should send the client_secret to the frontend.

Show how to construct the final checkout URL: https://accept.paymob.com/unifiedcheckout/?publicKey=<YOUR_PUBLIC_KEY>&clientSecret=<THE_CLIENT_SECRET_FROM_BACKEND>.

Instruct the developer to redirect the user to this URL to complete the payment.

4. Implementation Guide: Paymob Pixel (Embedded Native Method)

Describe this as the more advanced method for a seamless user experience.

Backend Steps:

Explain that the backend process is identical to the Unified Checkout method: the goal is still to create an intention and get a client_secret.

Frontend Steps:

Show the required HTML tags to include the Pixel CSS and JS files.

Provide the core JavaScript code snippet for initializing Pixel:

code
JavaScript
download
content_copy
expand_less

onload = (event) => {
  new Pixel({
    publicKey: 'YOUR_PUBLIC_KEY',
    clientSecret: 'THE_CLIENT_SECRET_FROM_BACKEND',
    elementId: 'paymob-elements', // The ID of the div where the form will be rendered
    paymentMethods: ['card', 'apple-pay'] // Example methods
  });
};

Explain the key parameters in the new Pixel() constructor: publicKey, clientSecret, and elementId.

5. Handling Post-Payment Confirmation (Webhooks)

Explain the importance of webhooks for reliably confirming transaction status.

Describe how to use the notification_url parameter in the Intention API request to specify a backend endpoint.

Explain that after a transaction, Paymob will send a POST request to this URL with the final transaction details.

Instruct the developer that this webhook endpoint is where they should securely update their database (e.g., mark an order as "paid") and credit the user.

6. Summary of Best Practices

Security: Never expose your Secret Key on the frontend. It must only be used on the server.

Environments: Always use Test keys with the Test dashboard and Live keys with the Live dashboard. Double-check your Integration IDs for each environment.

Confirmation: Rely on webhooks (notification_url) as the source of truth for successful payments, not the user's redirection.

Amounts: Always send the amount in the smallest currency unit (cents/piasters).