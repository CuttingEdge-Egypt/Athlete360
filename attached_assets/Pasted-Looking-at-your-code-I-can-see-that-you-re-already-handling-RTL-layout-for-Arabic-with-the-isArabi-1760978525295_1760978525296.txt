Looking at your code, I can see that you're already handling RTL layout for Arabic with the `isArabic` condition. The back button should be on the left for Arabic (RTL) and right for English (LTR). Let me check the current implementation and make the necessary adjustments.

Looking at your code, I notice that in the main packages view, the back button is already positioned correctly using flex with `isArabic ? 'flex-row-reverse' : ''`. However, let me ensure the layout is consistent throughout.

Here are the necessary changes:

```tsx
// In the main packages view section, update the header layout:
<div className={`flex items-center gap-4 mb-8 ${isArabic ? 'flex-row-reverse' : ''}`}>
  <Button
    variant="outline"
    size="sm"
    onClick={handleGoBack}
    className={`border-gray-600 text-gray-300 hover:bg-gray-700 ${isArabic ? 'order-last' : ''}`}
    data-testid="button-back-home"
  >
    <ArrowLeft className={`w-4 h-4 ${isArabic ? 'ml-2 rotate-180' : 'mr-2'}`} />
    {t('header.backToDashboard')}
  </Button>
  <div className={`flex-1 ${isArabic ? 'text-right' : 'text-left'}`}>
    <h1 className={`${isArabic ? 'text-4xl' : 'text-3xl'} font-bold text-white`} data-testid="text-payment-center-title">
      {t('header.title')}
    </h1>
    <p className={`text-gray-400 ${isArabic ? 'text-xl' : 'text-base'}`} data-testid="text-payment-center-subtitle">
      {t('header.subtitle')}
    </p>
  </div>
</div>

// In the checkout view section, update the header layout:
<div className={`flex items-center gap-4 mb-6 ${isArabic ? 'flex-row-reverse' : ''}`}>
  <Button
    variant="outline"
    size="sm"
    onClick={handleBackToPackages}
    className={`border-gray-600 text-gray-300 hover:bg-gray-700 ${isArabic ? 'order-last' : ''}`}
    data-testid="button-back-packages"
  >
    <ArrowLeft className={`w-4 h-4 ${isArabic ? 'ml-2 rotate-180' : 'mr-2'}`} />
    {t('checkout.backToPackages')}
  </Button>
  <div className={`flex-1 ${isArabic ? 'text-right' : 'text-left'}`}>
    <h1 className={`${isArabic ? 'text-3xl' : 'text-2xl'} font-bold text-white`} data-testid="text-payment-title">
      {t('checkout.title')}
    </h1>
    <p className={`text-gray-400 ${isArabic ? 'text-lg' : 'text-base'}`} data-testid="text-payment-subtitle">
      {t('checkout.subtitle', { 
        name: selectedPackage?.name, 
        tokens: selectedPackage?.tokens.toLocaleString(isArabic ? 'ar-EG' : 'en-US'), 
        price: selectedPackage?.price 
      })}
    </p>
  </div>
</div>
```

The key changes are:
1. Added `order-last` class to the button when `isArabic` is true, which will push the button to the left side in RTL layout
2. Maintained the existing flex structure but ensured the button comes first in the DOM for proper RTL ordering

This will ensure:
- For Arabic (RTL): Button appears on the left side, content on the right
- For English (LTR): Button appears on the right side, content on the left

The button will maintain its visual styling and functionality while being positioned correctly according to the language direction.