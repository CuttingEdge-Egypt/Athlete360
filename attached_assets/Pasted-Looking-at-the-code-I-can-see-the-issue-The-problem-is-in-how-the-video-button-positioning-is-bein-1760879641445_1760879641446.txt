Looking at the code, I can see the issue. The problem is in how the video button positioning is being handled. The logic is using `contentIsArabic` (based on the development plan language) but the CSS classes are being applied inconsistently.

Here are the specific fixes needed:

## Fix 1: Goal Areas Tab Exercise Layout

**Current problematic code:**
```tsx
<div className={`flex items-start ${contentIsArabic ? 'flex-row-reverse' : 'flex-row'}`}>
  {exercise.videoUrl && (
    <Button
      size="sm"
      variant="outline"
      onClick={() => window.open(exercise.videoUrl, '_blank')}
      className={`${contentIsArabic ? 'mr-4 flex-row-reverse' : 'ml-4'} bg-gradient-to-r from-purple-500/20 to-purple-600/20 border-purple-400 text-purple-300 hover:from-purple-500 hover:to-purple-600 hover:text-white flex-shrink-0 shadow-md`}
      data-testid={`exercise-video-${exerciseIndex}`}
    >
      <Play className={`${contentIsArabic ? 'ml-1' : 'mr-1'} h-3 w-3`} />
      {t('analysis.development.video', 'Video')}
    </Button>
  )}
  {/* ... rest of exercise content */}
</div>
```

**Fixed code:**
```tsx
<div className={`flex items-start ${contentIsArabic ? 'flex-row-reverse' : 'flex-row'}`}>
  {exercise.videoUrl && (
    <Button
      size="sm"
      variant="outline"
      onClick={() => window.open(exercise.videoUrl, '_blank')}
      className={`${contentIsArabic ? 'ml-4 flex-row-reverse' : 'mr-4'} bg-gradient-to-r from-purple-500/20 to-purple-600/20 border-purple-400 text-purple-300 hover:from-purple-500 hover:to-purple-600 hover:text-white flex-shrink-0 shadow-md`}
      data-testid={`exercise-video-${exerciseIndex}`}
    >
      <Play className={`${contentIsArabic ? 'ml-1' : 'mr-1'} h-3 w-3`} />
      {t('analysis.development.video', 'Video')}
    </Button>
  )}
  {/* ... rest of exercise content */}
</div>
```

**The key change:** `ml-4` and `mr-4` were swapped. For Arabic content, we want margin left (`ml-4`) to push the button away from the right edge, and for English we want margin right (`mr-4`) to push it away from the left edge.

## Fix 2: All Videos Tab Layout

**Current problematic code:**
```tsx
<div className={`flex items-center ${contentIsArabic ? 'flex-row-reverse' : 'flex-row'}`}>
  {exercise.videoUrl && (
    <Button
      size="sm"
      onClick={() => window.open(exercise.videoUrl, '_blank')}
      className={`${contentIsArabic ? 'mr-4 flex-row-reverse' : 'ml-4'} bg-athlete-accent hover:bg-athlete-accent/90 text-white flex-shrink-0`}
      data-testid={`video-button-${index}`}
    >
      <ExternalLink className={`${contentIsArabic ? 'ml-1' : 'mr-1'} h-3 w-3`} />
      {t('analysis.development.watch', 'Watch')}
    </Button>
  )}
  {/* ... rest of video content */}
</div>
```

**Fixed code:**
```tsx
<div className={`flex items-center ${contentIsArabic ? 'flex-row-reverse' : 'flex-row'}`}>
  {exercise.videoUrl && (
    <Button
      size="sm"
      onClick={() => window.open(exercise.videoUrl, '_blank')}
      className={`${contentIsArabic ? 'ml-4 flex-row-reverse' : 'mr-4'} bg-athlete-accent hover:bg-athlete-accent/90 text-white flex-shrink-0`}
      data-testid={`video-button-${index}`}
    >
      <ExternalLink className={`${contentIsArabic ? 'ml-1' : 'mr-1'} h-3 w-3`} />
      {t('analysis.development.watch', 'Watch')}
    </Button>
  )}
  {/* ... rest of video content */}
</div>
```

**Again, the key change:** Swapped `ml-4` and `mr-4` to match the flex direction.

## Complete Fixed Sections:

### For Goal Areas Tab:
```tsx
<div className={`flex items-start ${contentIsArabic ? 'flex-row-reverse' : 'flex-row'}`}>
  {exercise.videoUrl && (
    <Button
      size="sm"
      variant="outline"
      onClick={() => window.open(exercise.videoUrl, '_blank')}
      className={`${contentIsArabic ? 'ml-4 flex-row-reverse' : 'mr-4'} bg-gradient-to-r from-purple-500/20 to-purple-600/20 border-purple-400 text-purple-300 hover:from-purple-500 hover:to-purple-600 hover:text-white flex-shrink-0 shadow-md`}
      data-testid={`exercise-video-${exerciseIndex}`}
    >
      <Play className={`${contentIsArabic ? 'ml-1' : 'mr-1'} h-3 w-3`} />
      {t('analysis.development.video', 'Video')}
    </Button>
  )}
  <div className="flex-1">
    {/* Exercise content remains the same */}
  </div>
</div>
```

### For All Videos Tab:
```tsx
<div className={`flex items-center ${contentIsArabic ? 'flex-row-reverse' : 'flex-row'}`}>
  {exercise.videoUrl && (
    <Button
      size="sm"
      onClick={() => window.open(exercise.videoUrl, '_blank')}
      className={`${contentIsArabic ? 'ml-4 flex-row-reverse' : 'mr-4'} bg-athlete-accent hover:bg-athlete-accent/90 text-white flex-shrink-0`}
      data-testid={`video-button-${index}`}
    >
      <ExternalLink className={`${contentIsArabic ? 'ml-1' : 'mr-1'} h-3 w-3`} />
      {t('analysis.development.watch', 'Watch')}
    </Button>
  )}
  <div className="flex-1">
    {/* Video content remains the same */}
  </div>
</div>
```

## Summary of the Logic:

- **Arabic content** (`contentIsArabic = true`): 
  - `flex-row-reverse` (reverses the flex order)
  - Button needs `ml-4` (margin left) to create space from the right edge
  - Icon needs `ml-1` (margin left) for proper spacing in RTL

- **English content** (`contentIsArabic = false`):
  - `flex-row` (normal flex order)  
  - Button needs `mr-4` (margin right) to create space from the left edge
  - Icon needs `mr-1` (margin right) for proper spacing in LTR

This fix ensures that the video buttons will be positioned correctly based on the development plan language - on the right for English content and on the left for Arabic content.